Expose browser-safe `isArrayContaining`, `isObjectContaining`, `matchArrayContaining`, and `matchObject` compatibility helpers through the canonical branded asymmetric-matcher and equality engine. Define `@velocious/testing` as the owner of reusable assertion behavior while downstream frameworks retain only thin facades, preserving pass/fail semantics and package-canonical diagnostics without adding a Velocious dependency.
