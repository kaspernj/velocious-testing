Add a browser-safe deterministic JSON reporter with an explicit awaited writer and an additive `@velocious/testing/reporters` export. The Node CLI now selects `default` or `json` output through `--reporter`; completed JSON runs emit only the existing compact result on stdout, retain existing exit semantics, and route live console output to stderr with exact restoration, without changing protocol, result, or event shapes.
