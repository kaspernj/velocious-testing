Move framework-neutral discovery and strict CLI parsing, deterministic duration-aware sharding, profile collection/output, timing-manifest validation/hash/aggregation, generic console reporting/composition/slowest helpers, and standalone grouping/profile/merge integration into `@velocious/testing`. Add the browser-safe `./profiling` activity-name entry without changing the root/runner protocol or adding any Velocious dependency. Preserve per-callback profiling semantics and discovered-file ownership, close active attempts at finalization, combine line filters, validate safe integers, cover every supported fallback path, and merge structurally empty groups safely.
