Capture `it.each`, `test.each`, and `describe.each` declaration locations when their builders are created. Stored and reused builders retain that location for every generated row, so Node path-line selection at the builder line selects the generated tests or suite descendants. Registration and declaration validation remain deferred until invocation; child test locations and inline table behavior are preserved.
