Add the framework-neutral `suiteHookExecutor` runner option and Node forwarding so downstream adapters can wrap `beforeAll` and `afterAll` hooks and supply arguments through package-timeout-enforced `defaultExecute(args = [])`. The package retains hook order, reverse failure aggregation, phase attribution, active-suite tracking, and exactly-once interruption cleanup; standalone hooks still receive zero arguments, and protocol 1/schema 3 remain unchanged.
